split:
  test_size: 0.2
  random_state: 42

model:
  kind: "logreg"
  max_iter: 1000

autogluon:
  time_limit: 120
  presets: "medium_quality_faster_train"
  problem_type: "binary"
  label: "class"
  eval_metric: "f1"
  seed: 42
